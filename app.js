var options = {
    strings: ['Liders Education', 'The about learning is that nobody can take it away from you'],
    typeSpeed: 80,
    backSpeed: 35,
    startDelay: 1,
    smartBackspace: true,
    backDelay: 700,
    // fadeOut: true,
    // fadeOutClass: 'typed-fade-out',
    fadeOutDelay: 800,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
    contentType: 'html',
    onBegin: (self) => {'Muzaffar'},
  };
  var typed = new Typed('.element', options);


